export type C = {
    product_id: product
    prize_id: prize
}

type product = {

    name: string
    description: string
    image: string

}

type prize = {

    name: string
    description: string
    image: string
}